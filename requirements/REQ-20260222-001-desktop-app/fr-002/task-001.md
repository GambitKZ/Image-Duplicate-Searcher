## Task 001 — Add UI controls to MainPage.xaml

Goal

- Add UI controls for `ImageDirectory`, `OutputFilePath`, and `Supported Formats` to the desktop `MainPage.xaml`.

Deliverable

- A XAML patch that introduces labeled `Entry` fields and `Button` controls:
  - `Set Image Directory` button + `Entry` for `ImageDirectory`
  - `Set Output File` button + `Entry` for `OutputFilePath`
  - `Supported Formats` editable `Entry`

Estimate

- 60–90 minutes

Dependencies

- None
