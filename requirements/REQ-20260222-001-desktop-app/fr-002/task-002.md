## Task 002 — Implement folder/file pickers in MainPage.xaml.cs

Goal

- Implement the click handlers that open platform pickers and populate the corresponding `Entry` controls.

Deliverable

- `MainPage.xaml.cs` methods for:
  - `Set Image Directory` -> open folder picker, return path
  - `Set Output File` -> open save-file picker, return path
  - Proper fallback to manual `Entry` input when platform picker is unavailable

Estimate

- 1–2 hours

Dependencies

- Task 001
