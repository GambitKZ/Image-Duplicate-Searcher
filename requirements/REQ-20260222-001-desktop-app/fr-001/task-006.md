# Task 006 — Wire UI to Workflow and UiReporter

Goal
Wire `MainPage` handlers to construct `ImageDuplicationOptions`, start `ExecuteWorkflowAsync`, and subscribe to `UiReporter.OnLogMessage` to append messages to the Response area.

Deliverable
- Updated `MainPage.xaml.cs` with background task handling and UI log appending.
- Basic cancellation support via a `Cancel` button or similar.

Estimated time
1–2 hours

Dependencies
- Task 003
- Task 004
- Task 005
