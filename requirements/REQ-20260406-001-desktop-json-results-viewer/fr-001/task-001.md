# Task 001 — Define in-memory JSON result models

Goal
Create Application-layer models that map Reporter JSON output (hash, image path, sizeMB) and provide a stable in-memory representation for the Desktop session.

Deliverable
- Add model types in Application for duplicate result groups and images.
- Ensure model fields map directly to Reporter JSON payload structure.
- Keep models read-only with respect to source file semantics.

Estimated time
30–60 minutes

Dependencies
- None