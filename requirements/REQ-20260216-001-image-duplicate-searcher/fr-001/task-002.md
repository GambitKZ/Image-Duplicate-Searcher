# Task 002 — Create appsettings.json File

# Description
Create appsettings.json in the project root (src folder) with default configuration settings including image directories, thumbnail size (64x64), output file path, and supported formats (jpeg, jpg, png, bmp).

# Deliverable
appsettings.json file with initial configuration.

# Dependencies
None

# Implementation Notes
Set default values; make sure it's copied to output directory if needed.