# Task 007: Update Program.cs to use IReporter for reporting

Replace the current simple console output of groups with a call to the IReporter.ReportDuplicates method after grouping is complete.

Deliverable: Updated Program.cs to inject IReporter and use it for reporting instead of the existing foreach loop.