# Task 003: Create Reporter class implementing IReporter

Create the Reporter class in the Services folder that implements the IReporter interface. The class should accept ImageDuplicationOptions via dependency injection and use Spectre.Console for output.

Deliverable: New file Reporter.cs in src/Services/ implementing IReporter.