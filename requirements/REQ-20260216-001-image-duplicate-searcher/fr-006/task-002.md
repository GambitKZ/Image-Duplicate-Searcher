# Task 002: Create IReporter interface in Services folder

Create the IReporter interface in the Services folder to define the contract for reporting duplicate image groups. The interface should include a method to report the grouped duplicates.

Deliverable: New file IReporter.cs in src/Services/ with the interface definition.